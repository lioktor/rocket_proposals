import places from 'places.js';

const initAutocomplete = () => {
  const addressInput = document.getElementById('client_company_address');
  if (addressInput) {
    // places({ container: addressInput });
  }
};

export { initAutocomplete };
